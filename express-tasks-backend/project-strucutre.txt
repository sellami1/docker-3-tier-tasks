task-manager-backend/                  # Root folder of the backend project
│
├── config/                             # Configuration-related files
│   └── db.js                           # MongoDB connection logic
│
├── controllers/                        # Business logic for handling requests/responses
│   └── taskController.js
│
├── init/                               # Initialization scripts (useful later for Docker init)
│   └── init-db.js                      # MongoDB init script with sample data
│
├── models/                             # Mongoose schemas/models
│   └── task.js
│
├── routes/                             # Route definitions
│   └── taskRoutes.js
│
├── .dockerignore                       # Docker context exclusions (already prepared)
│
├── .env                                # Environment variables (NEVER commit – add to .gitignore)
│   # (create from .env.example below)
│
├── .env.example                        # Template for environment variables
│
├── .gitignore                          # Standard Node.js + env ignore rules
│
├── package.json                        # Dependencies and scripts
│
├── package-lock.json                   # (generated after npm install)
│
└── server.js                           # Main entry point – sets up Express app & starts server